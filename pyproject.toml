[project]
name = "devcourse-api-demo"
version = "0.1.0"
description = "A demo project for the DevCouRSE session on APIs in Python."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]==0.141.1",
    "sqlmodel==0.0.39",
]
